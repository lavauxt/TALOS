% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/main.R
\name{talos}
\alias{talos}
\title{TALOS wrapper with automatic exon fetching}
\usage{
talos(
  bam_path,
  gene,
  build = "hg19",
  padding = 500L,
  min_support = NULL,
  min_size = NULL,
  max_correction = NULL,
  plot = TRUE,
  sample_name = NULL,
  filter_intronic = NULL,
  ptd_mode = NULL,
  use_cigar_bp = NULL,
  refine_bp = NULL,
  min_mapq = NULL,
  min_wt_reads = NULL,
  cluster_tolerance = NULL,
  vaf_threshold = NULL,
  min_strand_bias = NULL,
  max_strand_bias = NULL,
  min_mean_support_mapq = NULL,
  max_breakpoint_spread = NULL,
  min_softclip_fraction = NULL,
  min_unique_breakpoints = NULL,
  min_coverage_drop = NULL,
  coverage_window = NULL,
  min_microhomology = NULL,
  min_discordant_ratio = NULL,
  min_entropy = NULL,
  detect_orientation = NULL,
  do_annotate_hotspots = NULL,
  hotspot_db_path = NULL,
  compute_alignment_score = TRUE,
  compute_support_bases = TRUE,
  compute_consistency = TRUE,
  compute_itd_coverage = TRUE,
  compute_coverage_drop = TRUE,
  compute_microhomology = TRUE,
  compute_repeat_entropy = TRUE,
  compute_discordant_ratio = TRUE,
  compute_hgvs = TRUE,
  min_itd_read_coverage = NULL,
  html_report = NULL,
  use_gviz = NULL,
  bsgenome = NULL,
  yaml_path = system.file("extdata", "gene_config.yaml", package = "TALOS"),
  add_config_to_report = FALSE,
  max_pairwise_alignments = NULL,
  output_prefix = "TALOS",
  include_sample = TRUE,
  include_gene = TRUE,
  include_timestamp = TRUE,
  output_sep = "_",
  global_log = TRUE,
  min_side_softclip_reads = 10,
  max_side_ratio = 10,
  min_softclip_pct_side = 1,
  min_left_softclip_pct_wt = 1,
  min_right_softclip_pct_wt = 1,
  ...
)
}
\arguments{
\item{bam_path}{BAM file context path.}

\item{gene}{Gene symbol (e.g., "FLT3")}

\item{build}{"hg19" or "hg38"}

\item{padding}{Base pairs mapped on bounds (default 500L).}

\item{compute_discordant_ratio}{Boolean: whether to compute discordant pair metric (default TRUE).}

\item{yaml_path}{Path to optional YAML config overrides}

\item{add_config_to_report}{Include config in plots/HTML report}

\item{max_pairwise_alignments}{Limit maximum sequences for robust alignment matching}

\item{output_prefix}{Base name prefix (default "TALOS")}

\item{include_sample}{Include sample name in filenames}

\item{include_gene}{Include gene name in filenames}

\item{include_timestamp}{Include timestamp in filenames}

\item{output_sep}{Separator for filename parts}

\item{global_log}{Write all logs to a single file}

\item{min_side_softclip_reads}{Minimum number of reads with soft‑clip on each side (default 10).}

\item{max_side_ratio}{Maximum ratio of left/right soft‑clip counts before filtering (default 10).}

\item{min_softclip_pct_side}{Minimum percentage of supporting reads with soft‑clip on each side (default 1.0).}

\item{...}{Extra settings.}
}
\description{
TALOS wrapper with automatic exon fetching
}
