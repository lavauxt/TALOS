% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engine_alu.R
\name{.extract_alu_candidates}
\alias{.extract_alu_candidates}
\title{Extract ALU-positive soft-clip candidates from a set of reads}
\usage{
.extract_alu_candidates(
  reads,
  alu_seqs,
  genomic_start,
  min_clip_len = 25L,
  min_alu_score = 0.6,
  verbose = FALSE
)
}
\arguments{
\item{reads}{GAlignments object (from .load_bam_data_streaming).}

\item{alu_seqs}{Named DNAStringSet.}

\item{genomic_start}{Integer – regional offset for local-coord conversion.}

\item{min_clip_len}{Minimum clip length (default 25 bp).}

\item{min_alu_score}{Minimum normalised alignment score (default 0.60).}

\item{verbose}{Print progress (default FALSE).}
}
\value{
data.frame with columns:
  read_name, genomic_pos, local_pos, clip_side, clip_seq,
  alu_subtype, alu_score, alu_strand, aln_start, aln_end,
  poly_a_len, mapq, is_reverse
}
\description{
For each read, both the leading and trailing soft-clips are checked
against every ALU consensus.  Reads with at least one matching clip
are retained with the best-hit metadata.
}
\keyword{internal}
