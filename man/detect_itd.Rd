% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/main.R
\name{detect_itd}
\alias{detect_itd}
\alias{talos}
\title{Detect ITDs/PTDs using the TALOS Algorithm}
\usage{
detect_itd(
  bam_path,
  gene_config,
  genomic_ref_seq = NULL,
  k = 11,
  min_support = 10,
  min_size = 15,
  max_missing_kmers = 0.5,
  cluster_tolerance = 10,
  prefilter = TRUE,
  search_window = 5000L,
  require_unique_anchor = FALSE,
  min_ins_filter = 3,
  output_prefix = "TALOS",
  output_folder = "./results",
  write_vcf = TRUE,
  verbose = TRUE,
  debug = FALSE,
  nominal_read_len = 150,
  max_correction = 2,
  plot = FALSE,
  sample_name = NULL,
  filter_intronic = FALSE,
  ptd_mode = FALSE,
  use_cigar_bp = TRUE,
  refine_bp = FALSE,
  min_mapq = 20,
  min_wt_reads = 0,
  vaf_threshold = 0.01,
  min_strand_bias = 0,
  max_strand_bias = 1,
  min_mean_support_mapq = 0,
  max_breakpoint_spread = Inf,
  min_softclip_fraction = 0,
  min_unique_breakpoints = 0,
  min_coverage_drop = 1,
  min_local_coverage = 0,
  coverage_window = 200,
  min_span_coverage = 0,
  min_span_depth_fold_change = 0,
  max_span_depth_fold_change = Inf,
  min_microhomology = 0,
  min_discordant_ratio = 0,
  min_entropy = 0,
  min_alignment_score = 0.2,
  detect_orientation = TRUE,
  do_annotate_hotspots = TRUE,
  hotspot_db_path = NULL,
  compute_alignment_score = TRUE,
  compute_support_bases = TRUE,
  compute_consistency = FALSE,
  compute_itd_coverage = TRUE,
  compute_coverage_drop = TRUE,
  compute_microhomology = TRUE,
  compute_repeat_entropy = TRUE,
  compute_discordant_ratio = TRUE,
  compute_hgvs = TRUE,
  html_report = TRUE,
  use_gviz = TRUE,
  min_itd_read_coverage = 50,
  max_reads_in_region = 2e+05,
  add_config_to_report = FALSE,
  max_pairwise_alignments = 30L,
  include_sample = TRUE,
  include_gene = TRUE,
  include_timestamp = TRUE,
  output_sep = "_",
  global_log = TRUE,
  min_side_softclip_reads = 0,
  max_side_ratio = 10,
  min_softclip_pct_side = 0,
  min_left_softclip_pct_wt = 0,
  min_right_softclip_pct_wt = 0,
  min_abs_side_softclip = 0,
  max_itd_length = 1000,
  convert_long_to_ptd = TRUE,
  min_length = NULL,
  max_length = NULL,
  use_kmers = TRUE,
  exon_padding = 0L,
  prefer_extended_length = TRUE,
  ptd_allow_asymmetric = TRUE,
  ptd_use_local_assembly = TRUE,
  merge_ptd_intervals = FALSE,
  merge_gap = 500L,
  min_ptd_length = 200L,
  max_plausible_fragments = 4L,
  use_exon_graph = FALSE,
  ...
)

talos(
  bam_path,
  gene,
  build = "hg19",
  padding = 500L,
  min_support = NULL,
  min_size = NULL,
  max_correction = NULL,
  max_missing_kmers = NULL,
  plot = TRUE,
  sample_name = NULL,
  filter_intronic = NULL,
  ptd_mode = NULL,
  use_cigar_bp = NULL,
  refine_bp = NULL,
  min_mapq = NULL,
  min_wt_reads = NULL,
  cluster_tolerance = NULL,
  search_window = NULL,
  require_unique_anchor = NULL,
  vaf_threshold = NULL,
  min_strand_bias = NULL,
  max_strand_bias = NULL,
  min_mean_support_mapq = NULL,
  max_breakpoint_spread = NULL,
  min_softclip_fraction = NULL,
  min_unique_breakpoints = NULL,
  min_coverage_drop = NULL,
  min_local_coverage = NULL,
  coverage_window = NULL,
  min_microhomology = NULL,
  min_span_coverage = NULL,
  min_span_depth_fold_change = NULL,
  max_span_depth_fold_change = NULL,
  min_discordant_ratio = NULL,
  min_entropy = NULL,
  min_alignment_score = NULL,
  detect_orientation = NULL,
  do_annotate_hotspots = NULL,
  hotspot_db_path = NULL,
  compute_alignment_score = TRUE,
  compute_support_bases = TRUE,
  compute_consistency = FALSE,
  compute_itd_coverage = TRUE,
  compute_coverage_drop = TRUE,
  compute_microhomology = TRUE,
  compute_repeat_entropy = TRUE,
  compute_discordant_ratio = TRUE,
  compute_hgvs = TRUE,
  min_itd_read_coverage = NULL,
  html_report = NULL,
  use_gviz = NULL,
  bsgenome = NULL,
  yaml_path = system.file("extdata", "gene_config.yaml", package = "TALOS"),
  add_config_to_report = FALSE,
  max_pairwise_alignments = NULL,
  output_prefix = "TALOS",
  include_sample = TRUE,
  include_gene = TRUE,
  include_timestamp = TRUE,
  output_sep = "_",
  global_log = TRUE,
  min_side_softclip_reads = NULL,
  max_side_ratio = NULL,
  min_softclip_pct_side = NULL,
  min_left_softclip_pct_wt = NULL,
  min_right_softclip_pct_wt = NULL,
  min_abs_side_softclip = NULL,
  max_itd_length = NULL,
  convert_long_to_ptd = NULL,
  min_length = NULL,
  max_length = NULL,
  use_kmers = TRUE,
  exon_padding = 0L,
  prefer_extended_length = NULL,
  ptd_allow_asymmetric = NULL,
  ptd_use_local_assembly = NULL,
  merge_ptd_intervals = NULL,
  merge_gap = NULL,
  min_ptd_length = NULL,
  max_plausible_fragments = NULL,
  use_exon_graph = NULL,
  verbose = TRUE,
  debug = FALSE,
  ...
)
}
\arguments{
\item{bam_path}{Path to an indexed BAM file.}

\item{gene_config}{Gene configuration list from \code{\link{get_gene_config}}.}

\item{genomic_ref_seq}{Optional override for the genomic reference sequence.}

\item{k}{K-mer size (default 11).}

\item{min_support}{Minimum support reads (default 10).}

\item{min_size}{Minimum duplication length (default 10).}

\item{max_missing_kmers}{Proportion threshold to drop reads.}

\item{cluster_tolerance}{Radius (bp) to cluster breakpoints.}

\item{prefilter}{Enable fast prefiltering of alignments.}

\item{search_window}{Max distance (bp) the split-read anchor search
(.estimate_itd_from_anchors()) looks for a re-anchor match. This is a
hard ceiling on any anchor-confirmed length estimate -- a true
duplication longer than this can only be sized via the noisier k-mer
walk fallback. Default 5000 (unchanged for FLT3-scale ITDs). KMT2A
widens this since its PTDs can genuinely exceed 5kb; safe to widen
because the anchor search now requires an unambiguous (single-hit)
match per prefix length rather than pooling every repeat-driven hit.}

\item{require_unique_anchor}{If TRUE, the anchor search
(.estimate_itd_from_anchors()) only trusts a prefix-length match when
it's unambiguous (exactly one hit in the window); ambiguous matches
contribute nothing rather than every hit. Default FALSE (original
behaviour: pool every hit, ambiguous or not). Motivated by KMT2A's
Alu-rich introns producing wildly inconsistent length estimates from
short-prefix repeat coincidences, but NOT proven safe for every gene --
combined with an anchor-preferring best_len (since reverted) it
regressed HORIZON FLT3's certified 300bp ITD to 260bp. Left FALSE
everywhere by default; enable per-gene only after checking real
results, the way every other threshold in this pipeline was tuned.}

\item{min_ins_filter}{Minimum net insertion length filter.}

\item{output_prefix}{Base name prefix (default "TALOS").}

\item{output_folder}{Output folder path.}

\item{write_vcf}{Generate VCF report.}

\item{verbose}{Logging enabled.}

\item{debug}{If TRUE, writes one line per candidate cluster.}

\item{nominal_read_len}{Assumed read length.}

\item{max_correction}{Size-bias correction cap.}

\item{plot}{Output PDF.}

\item{sample_name}{Extracted/assigned sample name.}

\item{filter_intronic}{Auto-drop non-exonic breakpoints.}

\item{ptd_mode}{Run PTD softclip-based parsing.}

\item{use_cigar_bp}{Rely on CIGAR instead of K-mers for BP refine.}

\item{refine_bp}{Enable secondary refinement loop.}

\item{min_mapq}{Filter param.}

\item{min_wt_reads}{Require baseline WT.}

\item{vaf_threshold}{Minimum relative allele frequency.}

\item{min_strand_bias}{Lower bound SB.}

\item{max_strand_bias}{Upper bound SB.}

\item{min_mean_support_mapq}{Strict metric cutoff.}

\item{max_breakpoint_spread}{Strict metric cutoff.}

\item{min_softclip_fraction}{Strict metric cutoff.}

\item{min_unique_breakpoints}{Strict metric cutoff.}

\item{min_coverage_drop}{Strict metric cutoff (default 1.5, adjust based on expected coverage).}

\item{coverage_window}{Flank (bp) used as the background window for SpanDepthFoldChange.}

\item{min_span_coverage}{Minimum depth required across the *entire* candidate span
(breakpoint to breakpoint+Length), not just at the breakpoint -- see
compute_span_coverage(). Default 0 (off). This is the filter meant to be
tuned per-gene for large PTDs (KMT2A); short ITDs (FLT3) have spans small
enough that LocalCoverage already covers this case.}

\item{min_span_depth_fold_change}{Minimum ratio of mean span depth to mean
flanking depth -- see compute_span_depth_fold_change(). Default 0 (off);
soft/informational signal, only enable after checking real numbers since
a low-VAF true event can also fail this.}

\item{max_span_depth_fold_change}{Maximum ratio of mean span depth to mean
flanking depth. Default Inf (off). A real heterozygous-ish duplication
should show roughly (1+VAF)-fold elevation, not many-fold -- a huge
ratio is Manta's "collapsed reference region" signature (multi-mapping
reads from elsewhere pooling into one locus), not a duplication.}

\item{min_microhomology}{Required MH length.}

\item{min_discordant_ratio}{Required ratio length (for filtering).}

\item{min_entropy}{Sequence repeat complexity baseline.}

\item{min_alignment_score}{Minimum sequence identity (0–1) between read-derived ITD sequence and the corresponding reference segment. Bypassed automatically for imputed sequences (default 0.2).}

\item{detect_orientation}{Check inverse loops.}

\item{hotspot_db_path}{Path to SQLite lookup DB.}

\item{compute_alignment_score}{Metric toggle.}

\item{compute_support_bases}{Metric toggle.}

\item{compute_consistency}{Metric toggle. Default FALSE: SupportConsistency
never gates .apply_filters() (purely informational) and is circular for
imputed/ref-derived PTD sequence (checking reads against a sequence
assembled from a subset of those same reads, or filled from reference).
AlignmentScore/RefMatch_Observed/ITDReadCoverage already cover this
ground without the circularity. Set TRUE to restore it for debugging.}

\item{compute_itd_coverage}{Metric toggle.}

\item{compute_coverage_drop}{Metric toggle.}

\item{compute_microhomology}{Metric toggle.}

\item{compute_repeat_entropy}{Metric toggle.}

\item{compute_discordant_ratio}{Metric toggle (boolean, default TRUE).}

\item{compute_hgvs}{Produce HGVS format (only works if transcript is assigned).}

\item{html_report}{HTML overview toggle.}

\item{use_gviz}{Use graphical views.}

\item{min_itd_read_coverage}{Minimum percentage of the ITD length that must be covered by supporting reads (default 50). Low values indicate the sequence was largely imputed rather than directly observed. KMT2A is automatically relaxed to 10 to accommodate large PTDs where only the junctions are read-covered.}

\item{max_reads_in_region}{Loading safeguard.}

\item{add_config_to_report}{Render configs in results.}

\item{max_pairwise_alignments}{Max limit for computationally heavy sub-alignments.}

\item{include_sample}{Include sample name in output filenames (default TRUE).}

\item{include_gene}{Include gene name in output filenames (default TRUE).}

\item{include_timestamp}{Include timestamp in output filenames (default TRUE).}

\item{output_sep}{Separator between filename parts (default "_").}

\item{global_log}{Write all logs (config, compute time, errors) to a single file (default TRUE).}

\item{min_side_softclip_reads}{Minimum number of reads with soft‑clip on each side (default 0, i.e. disabled).}

\item{max_side_ratio}{Maximum ratio of left/right soft‑clip counts before filtering (default 10).}

\item{min_softclip_pct_side}{Minimum percentage of supporting reads that must have a soft‑clip on each side (default 0, i.e. disabled).}

\item{min_left_softclip_pct_wt}{Minimum percentage of wildtype reads with left soft‑clip (default 0, i.e. disabled).}

\item{min_right_softclip_pct_wt}{Minimum percentage of wildtype reads with right soft‑clip (default 0, i.e. disabled).}

\item{min_abs_side_softclip}{Minimum absolute soft‑clip count on each side (default 0, i.e. disabled).}

\item{max_itd_length}{Maximum duplication length to consider as ITD; longer duplications are automatically converted to PTD (zero length) if \code{convert_long_to_ptd} is TRUE (default 1000).}

\item{convert_long_to_ptd}{If TRUE, duplications longer than \code{max_itd_length} are reported as PTDs (length 0). If FALSE, they are skipped (default TRUE).}

\item{min_length}{Minimum duplication length to report (NULL = no lower bound).}

\item{max_length}{Maximum duplication length to report (NULL = no upper bound).}

\item{use_kmers}{Enable k-mer based analysis (default TRUE).}

\item{exon_padding}{Number of flanking exons to add to the genomic window (default 0).}

\item{prefer_extended_length}{If TRUE and duplication length > nominal_read_len,
use extension-based ITD length (mode of itdsize_ext) and refined breakpoint
as the primary call. Default TRUE.}

\item{ptd_allow_asymmetric}{Allow PTD detection with soft-clips on only one side (default TRUE).}

\item{ptd_use_local_assembly}{Attempt to reconstruct duplication from soft-clips (default TRUE).}

\item{merge_ptd_intervals}{If TRUE, merge overlapping/adjacent large duplication intervals into a single PTD call (default FALSE).}

\item{merge_gap}{Maximum gap (bp) between intervals to merge (default 500).}

\item{min_ptd_length}{Minimum length (bp) for intervals to be considered for merging (default 200).}

\item{max_plausible_fragments}{If a merged interval pools more than this many
eligible fragments, it is flagged (\code{MergeFragmentWarning = TRUE})
rather than silently reported at full confidence: a single true PTD
has historically fragmented into at most ~2-3 rows pre-merge, so a
larger pool is more consistent with independent scattered signal
(e.g. repeat-driven misalignment) getting merged by genomic
proximity alone. Informational only -- does not remove rows from
the result. Default 4.}

\item{use_exon_graph}{If TRUE, additionally run the directed exon-graph PTD
reconstruction (requires exon annotations in gene_config$all_exons).
Graph-derived candidates supplement rather than replace the legacy
candidate extraction below; the two are combined and reconciled by
the merge_ptd_intervals step. No longer requires ptd_mode=TRUE.
Default FALSE.}

\item{...}{Other parameters passed to the underlying engine.}

\item{annotate_hotspots}{SQLite DB lookups.}
}
\value{
Data frame of ITD calls with diagnostic columns, invisibly.
}
\description{
Detect ITDs/PTDs using the TALOS Algorithm
}
