% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engine_alu.R
\name{talos_alu}
\alias{talos_alu}
\title{Simplified entry point for ALU insertion detection}
\usage{
talos_alu(
  bam_path,
  gene,
  build = "hg19",
  padding = 500L,
  consensus_fa = NULL,
  min_support = NULL,
  min_alu_score = NULL,
  min_clip_len = NULL,
  min_mapq = NULL,
  cluster_tolerance = NULL,
  vaf_threshold = NULL,
  min_tsd = NULL,
  sample_name = NULL,
  output_prefix = "TALOS_ALU",
  output_folder = "./results",
  html_report = TRUE,
  do_annotate_hotspots = TRUE,
  hotspot_db_path = NULL,
  yaml_path = system.file("extdata", "gene_config.yaml", package = "TALOS"),
  bsgenome = NULL,
  exon_padding = 0L,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{bam_path}{Path to an indexed BAM file.}

\item{gene}{Gene symbol (must be present in \code{yaml_path}).}

\item{build}{Genome build: \code{"hg19"} or \code{"hg38"}.}

\item{padding}{Padding around targeted exons (default 500).}

\item{consensus_fa}{Path to ALU consensus FASTA (default: bundled file).}

\item{min_support}{Minimum supporting reads per cluster (default 3).}

\item{min_alu_score}{Minimum normalised alignment score (default 0.60).}

\item{min_clip_len}{Minimum soft-clip length to attempt alignment (default 25).}

\item{min_mapq}{Minimum MAPQ (default 20).}

\item{cluster_tolerance}{Clustering radius in bp (default 15).}

\item{vaf_threshold}{Minimum allele frequency to report (default 0.01).}

\item{min_tsd}{Minimum TSD length; 0 disables (default 0).}

\item{sample_name}{Sample identifier; derived from BAM if NULL.}

\item{output_prefix}{Base prefix for output files (default "TALOS_ALU").}

\item{output_folder}{Output directory.}

\item{html_report}{Write a self-contained HTML report (default TRUE).}

\item{do_annotate_hotspots}{Run hotspot annotation (default TRUE).}

\item{hotspot_db_path}{Optional path to custom hotspot CSV.}

\item{yaml_path}{Path to gene config YAML (defaults to bundled file).}

\item{bsgenome}{Optional BSgenome object or package string.}

\item{exon_padding}{Number of flanking exons to add to the window.}

\item{verbose}{Print progress messages (default TRUE).}

\item{...}{Additional arguments forwarded to \code{detect_alu}.}
}
\value{
data.frame of ALU insertions (invisibly).
}
\description{
Calls \code{\link{get_gene_config}} then \code{\link{detect_alu}}.
Gene-specific ALU settings can be provided in \code{gene_config.yaml}
under a \code{gene_settings:} block with keys:
  \code{min_support}, \code{min_alu_score}, \code{min_clip_len},
  \code{cluster_tolerance}, \code{min_tsd}.
}
